name: ssh-connetion
on:
  pull_request:
    branchs: [dev]

jobs:
  ssh-connection17:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3.3.0
    - name: execute remote ssh
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.REMOTE_SSH_HOST }}
        username: ${{ secrets.REMOTE_SSH_USERNAME }}
        key: ${{ secrets.REMOTE_SSH_KEY }}
        port: ${{ secrets.REMOTE_SSH_PORT }}
        script: |
          whoami

    # - name: Create SSH Key
    #   run: | # access key를 actions 내부 워크스페이스에 pem파일로 저장하고 권한을 확보
    #     echo "${{ secrets.REMOTE_SSH_KEY }}" > ~/private-key.pem
    #     chmod 600 ~/private-key.pem
    # - name: Create SSH directory # .ssh 디렉토리 생성
    #   run: | 
    #     mkdir -p ~/.ssh
    # - name: Add known host # known host에 해당 키를 추가
    #   run: |
    #     ssh-keyscan ${{ secrets.REMOTE_HOST }} >> ~/.ssh/known_hosts
    # - name: Copy files to remote server # github repo의 모든 파일을 target 경로에 복사함.
    #   run: |
    #     cd ${{ github.workspace }}
    #     scp -i ~/private-key.pem -r ./* ${{ secrets.REMOTE_SSH_USERNAME }}@${{ secrets.REMOTE_SSH_HOST }}:${{ secrets.REMOTE_SSH_PORT }}
    #     echo "Copied files to remote server"